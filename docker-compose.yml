version: '2'
services:
  backend:
    build: ./server/
    ports:
      - "4000:4000"
    volumes:
      - ./server:/usr/src/app
  frontend:
    build: ./client/
    ports:
      - "3000:3000"
    volumes:
      - ./client:/usr/src/app
